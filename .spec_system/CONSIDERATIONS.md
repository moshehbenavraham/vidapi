# Considerations

> Institutional memory for AI assistants. Updated between phases via /carryforward.
> **Line budget**: 600 max | **Last updated**: Phase 00 (2026-05-05)

---

## Active Concerns

Items requiring attention in upcoming phases. Review before each session.

### Technical Debt
<!-- Max 5 items -->

*None yet - add items as technical debt accumulates.*

### External Dependencies
<!-- Max 5 items -->

- FFmpeg 6+ must be available in all environments (Docker handles this, but local dev needs manual install)
- Redis required for ARQ job queue (even in dev mode)

### Performance / Security
<!-- Max 5 items -->

- FFmpeg subprocess spawning needs resource limits (memory, CPU time, disk) to prevent abuse
- Remote asset fetching needs timeouts and size limits to prevent DoS

### Architecture
<!-- Max 5 items -->

- FFmpeg filter graph complexity scales non-linearly with track/clip count; may need chunked rendering for complex compositions
- Text rendering via Pillow has limited typography compared to browser-based rendering; may need Playwright path for `html` asset type later

---

## Lessons Learned

Proven patterns and anti-patterns. Reference during implementation.

### What Worked
<!-- Max 15 items -->

*None yet - add patterns that prove effective.*

### What to Avoid
<!-- Max 10 items -->

- Avoid MoviePy wrapper (abstracts away filter graph control needed for precise timing)
- Avoid synchronous rendering in API process (blocks the event loop)

### Tool/Library Notes
<!-- Max 5 items -->

- Shottower reference: in-memory job queue with 1s polling works for single-node but won't scale
- Video Artist API reference: Playwright browser capture is slow but produces excellent text rendering
- HyperFrames reference: streaming FFmpeg encode via image2pipe is faster than writing frames to disk

---

## Resolved

Recently closed items (buffer - rotates out after 2 phases).

| Phase | Item | Resolution |
|-------|------|------------|
| - | *No resolved items yet* | - |

---

*Auto-generated by /initspec. Updated by /carryforward between phases.*
